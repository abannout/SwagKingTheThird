apiVersion: v1
kind: Service
metadata:
  name: {{ include "player-swagkingthethird.fullname" . }}
  labels:
  {{- include "player-swagkingthethird.labels" . | nindent 4 }}
spec:
{{- range .Values.service.ports }}
  type: {{ .type }}
  ports:
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
      {{- end }}
  selector:
  {{- include "player-swagkingthethird.selectorLabels" . | nindent 4 }}
